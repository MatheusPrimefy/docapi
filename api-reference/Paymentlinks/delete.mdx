---
title: 'Delete payment link'
openapi: DELETE 'https://api.premepay.com/v1/stores/{storeId}/payment_links/{linkId}'
---

* Request type: **\[DELETE]**

```json
https://api.premepay.com/v1/stores/{storeId}/payment_links/{linkId}
```

**Responses**

* 200: OK
This is the response for cases where your request is successful.

```json
  {
      "message": "Card deleted"
  }
  ```

***

* 400: Bad RequestThis is the response for cases where the server does not process the request due to an error encountered. Some examples that may result in this error are an incorrectly typed URL, malformed syntax, or an invalid routing request.

```json
  {
    "type": "https://httpstatuses.com/400",
    "title": "Bad Request",
    "status": 400,
    "detail": null,
  }
  ```

* 401: UnauthorizedThis is the response for cases where the request was unsuccessful because it lacks valid authentication credentials for the target resource.
  If needed, you can review our [authentication process](https://www.notion.so/Authentication-c941a0cb8e2647b187d87fcf8a314b37?pvs=21).
  
  ```json
  {
    "type": "https://httpstatuses.com/401",
    "title": "Unauthorized",
    "status": 401,
    "detail": null,
    "instance": null,
    "extensions": {}
  }
  ```

* 403: Forbidden