---
title: 'Delete product'
openapi: DELETE 'https://api.premepay.com/v1/stores/{storeId}/products/{productId}'
---

* Request type: **\[DELETE]**

```json
https://api.premepay.com/v1/stores/{storeId}/products/{productId}
```

You have the option of permanently deleting a Product if it is not associated with sales. Images related to this Product will also be deleted. Be careful when performing this request as the action cannot be reversed. If the Product is associated with any sale, the Product's status will be Deleted and you will not be able to view it in searches or use it in future sales.

>
  ⚠️ **Attention:** This action is irreversible.


### **Responses**

* 204: No ContentThis is the answer for cases where your request is successful.

* 400: Bad RequestThis is the answer for cases where the server does not process the request due to an error encountered. Some examples that can result in this error are a misspelled URL, malformed syntax, or invalid routing request.
```json
  {
      "message": "product not found"
  }
  ```

* 401: UnauthorizedThis is the response for cases where the request was unsuccessful because it lacks valid authentication credentials for the target resource.
  If needed, you can review our [authentication process](https://www.notion.so/Authentication-c941a0cb8e2647b187d87fcf8a314b37?pvs=21).
  
  
  ```json

   {
    "error": {
      "message": "Unauthorized"
    } 
  }
  ```

* 403: ForbiddenThis is the response for cases where the request has been banned. This happens when the server can understand the request request, but does not authorize the issue of an approval response.

```json
  {
    "error": {
      "message": "Forbidden"      
    }
  }
  ```

* 500: Internal Server Error
This is the answer for cases where an internal server error occurs.

***