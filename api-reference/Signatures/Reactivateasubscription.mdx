---
title: 'Reactivate a subscription'
openapi: POST 'https://api.premepay.com/v1/stores/{storeId}/subscriptions/{:id}/reactivate'
---

* Request type: **\[POST]**

```json
https://api.premepay.com/v1/stores/{storeId}/subscriptions/{:id}/reactivate
```

> 💡 **Do you already have a Subscription Plan?**
You will be able to enroll users in Subscription Plans once you have an existing Subscription Plan.


After the subscription has been cancelled, it is possible to reactivate it. If the next billing date is not defined, it will be scheduled according to the frequency of the Plan associated with the subscription. Want to set the next billing date? No problem! Just send it in the body of the request, as shown below:

>💡 It is not possible to schedule a backdated charge. Future charges must be at least D+1.


### **Parameters List**

| Parameter | Type | Description | Required |
| --- | --- | --- | --- |
| `nextPayment` | datetime | Indicates when the next charge should be made after reactivation | No |

**Request example**

This is an example of how submitting your request to reactivate a subscription plan should look like.

- Request Body - Defining the future reactivation date
    
    ```json
    {
        "nextPayment": "2022-12-12"
    }
    ```
    

- Request Body - Sending the next billing date as null (next billing based on the frequency associated with the Plan)
    
    ```json
    {
        "nextPayment": null
    }
    ```
    

### **Responses**

- 200: Ok
    
    This is the answer in both scenarios for cases where your request succeeds.
    
    ```json
    {
        "id": 9541,
        "plan": "Plano Mensal",
        "nextPayment": "2023-01-09T10:46:18.9572172Z",
        "status": "Active"
    }
    ```
    

If your request has invalid creation parameters - for example, specifying an invalid field or value - you may receive an error response. Below is the complete list of possible error responses:

- 400: Bad Request
    
    This is the answer for cases where the server does not process the request due to an error encountered. Some examples that can result in this error are a misspelled URL, malformed syntax, or invalid routing request.
    
    ```json
    {
      "errors": [
        {
          "message": "Error description" 
        }
      ]
    }
    ```
    
- 401: Unauthorized
    
    This is the response for cases where the request was unsuccessful because it lacks valid authentication credentials for the target resource.
    If needed, you can review our [authentication process](https://www.notion.so/Authentication-c941a0cb8e2647b187d87fcf8a314b37?pvs=21).****
    
    ```json
    
    {
        "type": "https://httpstatuses.com/401",
        "title": "Unauthorized",
        "status": 401,
        "detail": null,
        "instance": null,
        "extensions": {}
    }
    ```
    
- 403: Forbidden
    
    This is the response for cases where the request has been banned. This happens when the server can understand the request request, but does not authorize the issue of an approval response.
    
    ```json
    {
        "type": "https://httpstatuses.com/403",
        "title": "Unauthorized",
        "status": 403,
        "detail": null,
        "instance": null,
        "extensions": {}
    }
    ```
    
- 500: Internal Server Error
    
    This is the answer for cases where an internal server error occurs.
    
    ```json
    {
        "type": "https://httpstatuses.com/500",
        "title": "Internal Server Error",
        "status": 500,
        "detail": null,
        "instance": null,
        "extensions": {}
    }
    ```
    

---

**Related Content**

---
