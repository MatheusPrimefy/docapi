---
title: "Delete card"
openapi: "DELETE https://api.premepay.com/v1/stores/:id/customers/:id/cards/:id "
---

* Request type: **\[DELETE]**

```json
https://api.premepay.com/v1/stores/:id/customers/:id/cards/:id
```

<aside>
  ⚠️ **Attention:** This action is irreversible.
</aside>

### **Responses**

If your request is successful, no information will be returned, only the status code 200 - OK.

* 200: OK
```json
  ```

* 401: UnauthorizedThis is the response for cases where the request was unsuccessful because it lacks valid authentication credentials for the target resource.
  If needed, you can review our [authentication process](https://www.notion.so/Authentication-c941a0cb8e2647b187d87fcf8a314b37?pvs=21).
  
  
  ```json
  {
    "type": "https://httpstatuses.com/401",
    "title": "Unauthorized",
    "status": 401,
    "detail": null,
    "instance": null,
    "extensions": {}
  }
  ```

* 403: Forbidden

```json
  {
    "type": "https://httpstatuses.com/403",
    "title": "Forbidden",
    "status": 403,
    "detail": null,
    "instance": null,
    "extensions": {}
  }
  ```

* 404: Not FoundThis is the response for cases where the card or consumer was not found. This is an HTTP response code that indicates that there was communication between the request and the server, but the server could not find what was requested.


```json
  {
  	"type": "https://tools.ietf.org/html/rfc7231#section-6.5.4",
  	"title": "Not Found",
  	"status": 404,
  	"traceId": "|75012adf-47c1fe46efb71345."
  }
  ```

* 500: Internal Server ErrorThis error is returned when unhandled data is encountered, such as sending a string in a numeric field or attempting to associate the card with a non-existent consumer.


```json
  {
  	"type": "https://httpstatuses.com/500",
  	"title": "Internal Server Error",
  	"status": 500,
  	"detail": null,
  	"instance": null,
  	"extensions": {}
  }
  ```

***